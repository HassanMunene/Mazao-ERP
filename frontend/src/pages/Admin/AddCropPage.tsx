import React from 'react';
import { CropForm } from '@/components/dashboard/crops/CropForm';

const AddCropPage: React.FC = () => {
    return <CropForm mode="create" />;
};

export default AddCropPage;